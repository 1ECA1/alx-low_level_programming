#include "main.h"

/**
 * _isalphan - check nif a character is alphabet
 * @c: The charac ter to be checked
 * Return: if the character is latter
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		return (0);
	else
		return (0);
}
