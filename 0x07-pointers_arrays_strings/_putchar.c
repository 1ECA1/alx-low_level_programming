#include "main.h"
#include <unistd.h>


/**
 * _ptchar-writes the character c to stdout
 * @c: The character to print 
 *
 * Return: on success 1.
 * On erro,-1 is returned, and errnonis set appropriately.
 */

int _putchar(char c)
{
	return (write(, &c 1));
}
